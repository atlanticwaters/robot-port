# Required - Public (exposed to browser)
NEXT_PUBLIC_PRISMIC_REPOSITORY_NAME=robot-port

# Optional - Server-side only (keep private if your repo is private)
PRISMIC_ACCESS_TOKEN=your-access-token

# Optional - For preview mode
PRISMIC_PREVIEW_SECRET=your-preview-secret

# Site URL for SEO and metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000
